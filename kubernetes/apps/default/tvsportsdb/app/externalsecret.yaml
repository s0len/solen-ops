---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tvsportsdb-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: tvsportsdb-secret
    template:
      data:
        # Database connection
        DATABASE_URL: "postgresql+asyncpg://{{ .TVSPORTSDB_POSTGRES_USER }}:{{ .TVSPORTSDB_POSTGRES_PASS }}@postgres16-rw.database.svc.cluster.local:5432/tvsportsdb"

        # JWT Secret Key
        SECRET_KEY: "{{ .TVSPORTSDB_SECRET_KEY }}"

        # R2/S3 Storage (reusing existing cloudnative-pg credentials)
        S3_ENDPOINT_URL: "${SECRET_R2_HOST}"
        S3_ACCESS_KEY: "{{ .CNPG_R2_ACCESS_KEY_ID }}"
        S3_SECRET_KEY: "{{ .CNPG_R2_SECRET_ACCESS_KEY }}"
        S3_BUCKET_NAME: "{{ .TVSPORTSDB_S3_BUCKET_NAME }}"

        # Postgres init container vars
        INIT_POSTGRES_DBNAME: tvsportsdb
        INIT_POSTGRES_HOST: postgres16-rw.database.svc.cluster.local
        INIT_POSTGRES_USER: "{{ .TVSPORTSDB_POSTGRES_USER }}"
        INIT_POSTGRES_PASS: "{{ .TVSPORTSDB_POSTGRES_PASS }}"
        INIT_POSTGRES_SUPER_PASS: "{{ .CNPG_SUPER_PASS }}"

        # The SportsDB API Key
        SPORTSDB_API_KEY: "{{ .TVSPORTSDB_SPORTSDB_API_KEY }}"
  dataFrom:
    - extract:
        key: cloudnative-pg
      rewrite:
        - regexp:
            source: "(.*)"
            target: "CNPG_$1"

    - extract:
        key: tvsportsdb
      rewrite:
        - regexp:
            source: "(.*)"
            target: "TVSPORTSDB_$1"
