---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/prometheusrule_v1.json
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: volsync
spec:
  groups:
    - name: volsync.rules
      rules:
        - alert: VolSyncComponentAbsent
          expr: |
            absent(up{job="volsync-metrics"})
          annotations:
            summary: >-
              VolSync component has disappeared from Prometheus target discovery
          for: 5m
          labels:
            severity: critical

        # ReplicationDestinations are restore-only (manual trigger, no
        # schedule) — they don't have a meaningful "out of sync" concept.
        # Restrict the alert to ReplicationSources only.
        - alert: VolSyncVolumeOutOfSync
          expr: |
            volsync_volume_out_of_sync{role="source"} == 1
          annotations:
            summary: >-
              {{ $labels.obj_namespace }}/{{ $labels.obj_name }} volume is out of sync
          for: 15m
          labels:
            severity: critical
